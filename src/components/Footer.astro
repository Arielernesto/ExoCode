---
// import { motion } from 'framer-motion'
// import a from 'next/a'
// import { Facebook, Twitter, Instagram, aedin } from 'lucide-react'

const currentYear = new Date().getFullYear()
---
    <footer class="bg-gray-100 dark:bg-gray-900">
      <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div class="col-span-1 md:col-span-2">
            <a href="/" class="text-2xl font-bold text-purple-600 dark:text-purple-400">
              StartupX
            </a>
            <p class="mt-4 text-gray-600 dark:text-gray-300">
              Empowering startups to innovate, grow, and succeed in the digital age.
            </p>
          </div>
          <div>
            <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider">Resources</h3>
            <ul class="mt-4 space-y-4">
              {['Documentation', 'Tutorials', 'Pricing', 'FAQ'].map((item) => (
                <li>
                  <a href="#" class="text-base text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400">
                    {item}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider">Company</h3>
            <ul class="mt-4 space-y-4">
              {['About', 'Blog', 'Jobs', 'Press', 'Partners'].map((item) => (
                <li>
                  <a href="#" class="text-base text-gray-600 dark:text-gray-300 hover:text-purple-600 dark:hover:text-purple-400">
                    {item}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>
        <div class="mt-8 border-t border-gray-200 dark:border-gray-700 pt-8 flex flex-col md:flex-row justify-between items-center">
          <p class="text-base text-gray-500 dark:text-gray-400">
            &copy; {currentYear} StartupX. All rights reserved.
          </p>
          <!-- <div class="flex space-x-6 mt-4 md:mt-0">
            {[
              { icon: Facebook, label: 'Facebook' },
              { icon: Twitter, label: 'Twitter' },
              { icon: Instagram, label: 'Instagram' },
              { icon: aedin, label: 'aedIn' },
            ].map((item) => (
              <motion.a
                key={item.label}
                href="#"
                class="text-gray-500 dark:text-gray-400 hover:text-purple-600 dark:hover:text-purple-400"
                whileHover={{ scale: 1.1 }}
                whileTap={{ scale: 0.9 }}
                aria-label={item.label}
              >
                <item.icon class="h-6 w-6" />
              </motion.a>
            ))}
          </div> -->
        </div>
      </div>
    </footer>
