---
// Tecnologías destacadas con efectos modernos
const technologies = [
  {
    name: 'TypeScript',
    logo: '/ts.svg',
    description: 'TypeScript se está convirtiendo rápidamente en el estándar para el desarrollo de aplicaciones JavaScript a gran escala. Descubre por qué cada vez más desarrolladores están adoptando TypeScript y cómo puede mejorar tu flujo de trabajo.',
    color: 'from-blue-500/20 to-blue-600/10'
  },
  {
    name: 'Astro',
    logo: '/astro-logo.svg',
    description: 'Astro permite crear sitios web modernos con menos JavaScript y mejor rendimiento. Explora cómo este innovador framework está cambiando el paradigma del desarrollo web.',
    color: 'from-orange-500/20 to-purple-600/10'
  },
  {
    name: 'Svelte',
    logo: '/svelte-logo.svg',
    description: 'Svelte redefine la forma en que construimos interfaces reactivas, eliminando el virtual DOM y generando código optimizado en tiempo de compilación. Aprende sobre sus ventajas y cómo integrarlo en tus proyectos.',
    color: 'from-red-500/20 to-orange-600/10'
  }
];
---

<section class="mb-12">
  <h2 class="text-3xl font-bold relative inline-block mb-8">
    <span class="relative z-10">Tecnologías Destacadas</span>
    <span class="absolute bottom-0 left-0 w-full h-3 bg-primary/20 -z-0"></span>
  </h2>

  <div class="grid md:grid-cols-3 gap-6">
    {technologies.map((tech, index) => (
      <div class={`tech-card rounded-xl overflow-hidden p-6 cursor-pointer transition-all duration-500 hover:shadow-lg bg-gradient-to-br ${tech.color} border border-gray-100 dark:border-gray-700 relative group`}>
        <div class="absolute inset-0 bg-white dark:bg-gray-800 opacity-90 dark:opacity-90 transition-opacity duration-500 group-hover:opacity-95"></div>
        
        <div class="relative z-10">
          <div class="flex items-center mb-4">
            <img 
              src={tech.logo} 
              alt={`${tech.name} Logo`} 
              width={60} 
              height={60} 
              class="mr-4 transition-transform duration-500 group-hover:scale-110"
            />
            <h3 class="text-xl font-semibold text-gray-800 dark:text-white">{tech.name}</h3>
          </div>
          
          <p class="text-gray-600 dark:text-gray-300">
            {tech.description}
          </p>
          
          <div class="mt-4 flex justify-end">
            <a href={`/blog/tag/${tech.name.toLowerCase()}`} class="text-primary font-medium inline-flex items-center text-sm hover:underline">
              Ver artículos
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 ml-1 transition-transform duration-300 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    ))}
  </div>
</section>

<style>
  .tech-card {
    min-height: 240px;
    transform: translateY(0);
    transition: transform 0.5s ease;
  }
  
  .tech-card:hover {
    transform: translateY(-5px);
  }
  
  @media (prefers-reduced-motion) {
    .tech-card {
      transition: none;
    }
  }
</style>

